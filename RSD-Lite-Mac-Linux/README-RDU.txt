- I fix the error in the script

- I move the binaries (rename them) because:
  - I want to use the newest fastboot available for debian, so I simlink
  - The script does not use adb
  - I do not want to call the mac binaries accidentally
